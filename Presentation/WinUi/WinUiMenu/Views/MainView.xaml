<?xml version="1.0" encoding="utf-8"?>
<windowex:WindowEx x:Class="CiccioGest.Presentation.WinUiMenu.Views.MainView"
                   xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
                   xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
                   xmlns:local="using:CiccioGest.Presentation.WinUiMenu.Views"
                   xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
                   xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
                   xmlns:windowex="using:WinUIEx"
                   mc:Ignorable="d"
                   Activated="WindowEx_Activated"
                   Closed="WindowEx_Closed"
                   Title="MainView">

    <windowex:WindowEx.SystemBackdrop>
        <MicaBackdrop />
    </windowex:WindowEx.SystemBackdrop>

    <Grid>
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition />
        </Grid.RowDefinitions>

        <MenuBar>
            <MenuBarItem Title="Home">
                <MenuFlyoutItem Text="Apri Dashboard"
                                Command="{x:Bind ViewModel.ApriDashboardCommand, Mode=OneWay}" />
                <MenuFlyoutItem Text="Impostazioni"
                                Command="{x:Bind ViewModel.ApriSettingsCommand, Mode=OneWay}" />
            </MenuBarItem>
            <MenuBarItem Title="Fatture">
                <MenuFlyoutItem Text="Apri Fattura"
                                Command="{x:Bind ViewModel.ApriFatturaCommand, Mode=OneWay}" />
                <MenuFlyoutItem Text="Nuova Fattura"
                                Command="{x:Bind ViewModel.NuovaFatturaCommand, Mode=OneWay}" />
            </MenuBarItem>
            <MenuBarItem Title="Magazino">
                <MenuFlyoutItem Text="Articoli"
                                Command="{x:Bind ViewModel.ApriArticoliCommand, Mode=OneWay}" />
                <MenuFlyoutItem Text="Categorie"
                                Command="{x:Bind ViewModel.ApriCategorieCommand, Mode=OneWay}" />
            </MenuBarItem>
            <MenuBarItem Title="Anagrafica">
                <MenuFlyoutItem Text="Clienti"
                                Command="{x:Bind ViewModel.ApriClientiCommand, Mode=OneWay}" />
                <MenuFlyoutItem Text="Fornitori"
                                Command="{x:Bind ViewModel.ApriFornitoriCommand, Mode=OneWay}" />
            </MenuBarItem>
            <MenuBarItem Title="Help">
                <MenuFlyoutItem Text="About" />
            </MenuBarItem>
        </MenuBar>

        <Grid Grid.Row="1">
            <ContentControl x:Name="contentControl" />
        </Grid>
    </Grid>

</windowex:WindowEx>
