<UserControl x:Class="CiccioGest.Presentation.WpfApp.Test.CategoriaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CiccioGest.Presentation.WpfApp.Test"
             xmlns:converters="clr-namespace:CiccioGest.Presentation.WpfApp.Converters"
             mc:Ignorable="d"
             d:DesignHeight="450"
             d:DesignWidth="800"
             Width="800"
             Height="450"
             Background="#FFF7F7F7">

    <!--xmlns:converters="clr-namespace:TuoNamespace.Converters"-->

    <UserControl.Resources>
        <Color x:Key="AccentColor">#2B6FA6</Color>
        <SolidColorBrush x:Key="AccentBrush"
                         Color="{StaticResource AccentColor}" />

        <Style x:Key="PageTitleText"
               TargetType="TextBlock">
            <Setter Property="FontSize"
                    Value="20" />
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="Foreground"
                    Value="{StaticResource AccentBrush}" />
        </Style>

        <Style x:Key="HeaderText"
               TargetType="TextBlock">
            <Setter Property="FontSize"
                    Value="16" />
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="Foreground"
                    Value="{StaticResource AccentBrush}" />
        </Style>

        <Style x:Key="LabelStyle"
               TargetType="TextBlock">
            <Setter Property="FontSize"
                    Value="12" />
            <Setter Property="VerticalAlignment"
                    Value="Center" />
            <Setter Property="Margin"
                    Value="0,0,6,0" />
        </Style>
        
        <Style x:Key="TextBoxStyle"
               TargetType="TextBox">
            <Setter Property="Padding"
                    Value="6,4" />
            <Setter Property="MinHeight"
                    Value="26" />
            <Setter Property="BorderBrush"
                    Value="#FFBFBFBF" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="Background"
                    Value="White" />
        </Style>
        <Style x:Key="CommandButton"
               TargetType="Button">
            <Setter Property="Padding"
                    Value="10,6" />
            <Setter Property="Margin"
                    Value="6,0,0,0" />
            <Setter Property="Background"
                    Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground"
                    Value="White" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="MinWidth"
                    Value="90" />
            <Setter Property="Cursor"
                    Value="Hand" />
        </Style>
        <Style x:Key="SecondaryButton"
               TargetType="Button"
               BasedOn="{StaticResource CommandButton}">
            <Setter Property="Background"
                    Value="#FFF0F0F0" />
            <Setter Property="Foreground"
                    Value="#333" />
            <Setter Property="BorderBrush"
                    Value="#FFD0D0D0" />
            <Setter Property="BorderThickness"
                    Value="1" />
        </Style>

        <converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
    </UserControl.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <!-- toolbar -->
            <RowDefinition Height="Auto" />
            <!-- editor corrente -->
            <RowDefinition Height="*" />
            <!-- lista -->
            <RowDefinition Height="Auto" />
            <!-- footer -->
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <DockPanel Grid.Row="0"
                   LastChildFill="False"
                   Margin="0,0,0,10">

            <!-- Titolo -->
            <TextBlock Text="Categorie"
                       Style="{StaticResource PageTitleText}"
                       DockPanel.Dock="Left"
                       VerticalAlignment="Center"
                       Margin="0,0,20,0" />

            <!-- Pulsanti -->
            <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Left">
                <Button Style="{StaticResource CommandButton}"
                        Command="{Binding NuovaCategoriaCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                   Text=""
                                   Margin="0,0,6,0" />
                        <TextBlock Text="Nuova" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource CommandButton}"
                        Command="{Binding SalvaCategoriaCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                   Text=""
                                   Margin="0,0,6,0" />
                        <TextBlock Text="Salva" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource SecondaryButton}"
                        Command="{Binding EliminaCategoriaCommand}">
                    <!--<StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                   Text=""
                                   Margin="0,0,6,0" />
                        <TextBlock Text="Elimina" />
                    </StackPanel>-->
                    <TextBlock>
                        <Run Text="&#xE74D;"
                             FontFamily="Segoe MDL2 Assets" />
                        <Run Text="Elimina" />
                    </TextBlock>
                </Button>
            </StackPanel>

            <!-- Ricerca -->
            <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Right">
                <TextBlock Text="Cerca "
                           VerticalAlignment="Center" />
                <TextBox Width="200"
                         VerticalAlignment="Center"
                         Style="{StaticResource TextBoxStyle}"
                         Text="{Binding FiltroCategorie, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </StackPanel>


        </DockPanel>

        <!-- Editor corrente -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="6"
                Padding="12"
                BorderBrush="#FFE5E5E5"
                BorderThickness="1"
                Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="130" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Grid.ColumnSpan="2"
                           Style="{StaticResource HeaderText}"
                           Text="Categoria Corrente"
                           Margin="0,0,0,12" />

                <StackPanel Grid.Row="0"
                            Grid.Column="2"
                            Orientation="Horizontal"
                            HorizontalAlignment="Right"
                            DockPanel.Dock="Right">
                    <TextBlock VerticalAlignment="Center"
                               Foreground="#666"
                               Margin="0,0,8,0">ID:</TextBlock>
                    <TextBlock VerticalAlignment="Center"
                               FontWeight="SemiBold"
                               Text="{Binding CategoriaCorrente.Id}" />
                </StackPanel>

                <TextBlock Grid.Row="1"
                           Grid.Column="0"
                           Style="{StaticResource LabelStyle}"
                           Text="Nome:" />
                <TextBox Grid.Row="1"
                         Grid.Column="1"
                         Grid.ColumnSpan="2"
                         Style="{StaticResource TextBoxStyle}"
                         Text="{Binding CategoriaCorrente.Nome, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
        </Border>

        <!-- Lista categorie -->
        <Border Grid.Row="2"
                Background="White"
                CornerRadius="6"
                Padding="12"
                BorderBrush="#FFE5E5E5"
                BorderThickness="1">
            <Grid>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="*" />
                </Grid.RowDefinitions>

                <TextBlock Grid.Row="0"
                           Style="{StaticResource HeaderText}"
                           Text="Lista Categorie"
                           Margin="0,0,0,12" />

                <ListBox Grid.Row="1"
                         ItemsSource="{Binding Categorie}"
                         SelectedItem="{Binding SelectedCategoria, Mode=TwoWay}"
                         DisplayMemberPath="Nome"
                         HorizontalContentAlignment="Stretch" />
            </Grid>
        </Border>

        <!-- Footer -->
        <Grid Grid.Row="3"
              Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock VerticalAlignment="Center"
                       Foreground="#666"
                       Text="{Binding StatusMessage}" />
        </Grid>

        <!-- Busy overlay -->
        <!--<Grid Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}}">
            <Rectangle Fill="#80000000" />
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <ProgressBar Width="200"
                             Height="16"
                             IsIndeterminate="True" />
                <TextBlock Foreground="White"
                           Margin="0,8,0,0"
                           Text="Operazione in corso..."
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>-->
    </Grid>

</UserControl>
    
