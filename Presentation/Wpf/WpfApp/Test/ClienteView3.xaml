<Window x:Class="CiccioGest.Presentation.WpfApp.Test.ClienteView3"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
        xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
        xmlns:local="clr-namespace:CiccioGest.Presentation.WpfApp.Test"
        xmlns:Converters="clr-namespace:CiccioGest.Presentation.WpfApp.Converters"
        mc:Ignorable="d"
        Title="Cliente"
        Width="800"
        Height="450"
        ResizeMode="CanResize"
        WindowStartupLocation="CenterScreen"
        FontFamily="Segoe UI"
        Background="#FFF7F7F7">

    <Window.Resources>
        <Converters:BoolToVisibilityConverter x:Key="BoolToVisibilityConverter" />
        <Converters:IndirizzoToStringConverter x:Key="IndirizzoToStringConverter" />

        <!-- Colori e spaziatura -->
        <Color x:Key="AccentColor">#2B6FA6</Color>
        <SolidColorBrush x:Key="AccentBrush"
                         Color="{StaticResource AccentColor}" />
        <Style x:Key="HeaderText"
               TargetType="TextBlock">
            <Setter Property="FontSize"
                    Value="16" />
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="Foreground"
                    Value="{StaticResource AccentBrush}" />
        </Style>

        <Style x:Key="LabelStyle"
               TargetType="TextBlock">
            <Setter Property="FontSize"
                    Value="12" />
            <Setter Property="VerticalAlignment"
                    Value="Center" />
            <Setter Property="Margin"
                    Value="0,0,6,0" />
        </Style>

        <Style x:Key="TextBoxStyle"
               TargetType="TextBox">
            <Setter Property="Padding"
                    Value="6,4" />
            <Setter Property="MinHeight"
                    Value="26" />
            <Setter Property="BorderBrush"
                    Value="#FFBFBFBF" />
            <Setter Property="BorderThickness"
                    Value="1" />
            <Setter Property="Background"
                    Value="White" />
        </Style>

        <!-- Simple validation template -->
        <ControlTemplate x:Key="ValidationTemplate">
            <DockPanel LastChildFill="True">
                <Border BorderBrush="Red"
                        BorderThickness="1"
                        CornerRadius="2">
                    <AdornedElementPlaceholder />
                </Border>
            </DockPanel>
        </ControlTemplate>

        <!-- Button style -->
        <Style x:Key="CommandButton"
               TargetType="Button">
            <Setter Property="Padding"
                    Value="10,6" />
            <Setter Property="Margin"
                    Value="6,0,0,0" />
            <Setter Property="Background"
                    Value="{StaticResource AccentBrush}" />
            <Setter Property="Foreground"
                    Value="White" />
            <Setter Property="BorderThickness"
                    Value="0" />
            <Setter Property="FontWeight"
                    Value="SemiBold" />
            <Setter Property="MinWidth"
                    Value="80" />
            <Setter Property="Cursor"
                    Value="Hand" />
        </Style>

        <Style x:Key="SecondaryButton"
               TargetType="Button"
               BasedOn="{StaticResource CommandButton}">
            <Setter Property="Background"
                    Value="#FFF0F0F0" />
            <Setter Property="Foreground"
                    Value="#333" />
            <Setter Property="BorderBrush"
                    Value="#FFD0D0D0" />
            <Setter Property="BorderThickness"
                    Value="1" />
        </Style>
    </Window.Resources>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
            <RowDefinition Height="Auto" />
        </Grid.RowDefinitions>

        <!-- Toolbar -->
        <DockPanel Grid.Row="0"
                   LastChildFill="False"
                   Margin="0,0,0,10">
            <StackPanel Orientation="Horizontal"
                        DockPanel.Dock="Left">
                <Button Style="{StaticResource CommandButton}"
                        Command="{Binding NuovoCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                   Text=""
                                   Margin="0,0,6,0" />
                        <TextBlock Text="Nuovo" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource CommandButton}"
                        Command="{Binding SalvaCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                   Text=""
                                   Margin="0,0,6,0" />
                        <TextBlock Text="Salva" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource SecondaryButton}"
                        Command="{Binding ApriCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                   Text=""
                                   Margin="0,0,6,0" />
                        <TextBlock Text="Apri" />
                    </StackPanel>
                </Button>
                <Button Style="{StaticResource SecondaryButton}"
                        Command="{Binding EliminaCommand}">
                    <StackPanel Orientation="Horizontal">
                        <TextBlock FontFamily="Segoe MDL2 Assets"
                                   Text=""
                                   Margin="0,0,6,0" />
                        <TextBlock Text="Elimina" />
                    </StackPanel>
                </Button>
            </StackPanel>

            <!-- Status / Id -->
            <StackPanel Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        DockPanel.Dock="Right">
                <TextBlock VerticalAlignment="Center"
                           Foreground="#666"
                           Margin="0,0,8,0">ID:</TextBlock>
                <TextBlock VerticalAlignment="Center"
                           FontWeight="SemiBold"
                           Text="{Binding Cliente.Id}" />
            </StackPanel>
        </DockPanel>

        <!-- Content area -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="6"
                Padding="12"
                BorderBrush="#FFE5E5E5"
                BorderThickness="1">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="2*" />
                    <ColumnDefinition Width="1*" />
                </Grid.ColumnDefinitions>

                <!-- Dati Cliente -->
                <StackPanel Grid.Column="0"
                            Margin="0,0,12,0">
                    <TextBlock Style="{StaticResource HeaderText}"
                               Text="Dati Cliente"
                               Margin="0,0,0,12" />
                    <Grid Margin="0,0,0,6">
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="120" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0" Grid.Column="0"
                                   Style="{StaticResource LabelStyle}"
                                   Text="Nome:" />
                        <TextBox Grid.Row="0" Grid.Column="1"
                                 Style="{StaticResource TextBoxStyle}"
                                 Text="{Binding Cliente.Nome, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged, ValidatesOnDataErrors=True, ValidatesOnExceptions=True}"
                                 Validation.ErrorTemplate="{StaticResource ValidationTemplate}" />

                        <TextBlock Grid.Row="1" Grid.Column="0"
                                   Style="{StaticResource LabelStyle}"
                                   Text="Cognome:"
                                   Margin="0,8,0,0" />
                        <TextBox Grid.Row="1" Grid.Column="1"
                                 Style="{StaticResource TextBoxStyle}"
                                 Margin="0,8,0,0"
                                 Text="{Binding Cliente.Cognome, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="2" Grid.Column="0"
                                   Style="{StaticResource LabelStyle}"
                                   Text="Data Nascita:"
                                   Margin="0,8,0,0" />
                        <DatePicker Grid.Row="2" Grid.Column="1"
                                    Margin="0,8,0,0"
                                    SelectedDate="{Binding Cliente.DataNascita, Mode=TwoWay}" />

                        <TextBlock Grid.Row="3" Grid.Column="0"
                                   Style="{StaticResource LabelStyle}"
                                   Text="Telefono:"
                                   Margin="0,8,0,0" />
                        <TextBox Grid.Row="3" Grid.Column="1"
                                 Margin="0,8,0,0"
                                 Style="{StaticResource TextBoxStyle}"
                                 Text="{Binding Cliente.Telefono, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="4" Grid.Column="0"
                                   Style="{StaticResource LabelStyle}"
                                   Text="Email:"
                                   Margin="0,8,0,0" />
                        <TextBox Grid.Row="4" Grid.Column="1"
                                 Margin="0,8,0,0"
                                 Style="{StaticResource TextBoxStyle}"
                                 Text="{Binding Cliente.Email, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>
                </StackPanel>

                <!-- Dati Indirizzo -->
                <StackPanel Grid.Column="1">
                    <TextBlock Style="{StaticResource HeaderText}"
                               Text="Indirizzo"
                               Margin="0,0,0,12" />
                    <Grid>
                        <Grid.RowDefinitions>
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                            <RowDefinition Height="Auto" />
                        </Grid.RowDefinitions>
                        <Grid.ColumnDefinitions>
                            <ColumnDefinition Width="60" />
                            <ColumnDefinition Width="*" />
                        </Grid.ColumnDefinitions>

                        <TextBlock Grid.Row="0"
                                   Grid.Column="0"
                                   Style="{StaticResource LabelStyle}"
                                   Text="Via:" />
                        <TextBox Grid.Row="0"
                                 Grid.Column="1"
                                 Style="{StaticResource TextBoxStyle}"
                                 Text="{Binding Indirizzo.Via, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="1"
                                   Grid.Column="0"
                                   Style="{StaticResource LabelStyle}"
                                   Text="Civico:"
                                   Margin="0,8,0,0" />
                        <TextBox Grid.Row="1"
                                 Grid.Column="1"
                                 Margin="0,8,0,0"
                                 Style="{StaticResource TextBoxStyle}"
                                 Text="{Binding Indirizzo.Civico, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="2"
                                   Grid.Column="0"
                                   Style="{StaticResource LabelStyle}"
                                   Text="CAP:"
                                   Margin="0,8,0,0" />
                        <TextBox Grid.Row="2"
                                 Grid.Column="1"
                                 Margin="0,8,0,0"
                                 Style="{StaticResource TextBoxStyle}"
                                 Text="{Binding Indirizzo.CAP, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="3"
                                   Grid.Column="0"
                                   Style="{StaticResource LabelStyle}"
                                   Text="Città:"
                                   Margin="0,8,0,0" />
                        <TextBox Grid.Row="3"
                                 Grid.Column="1"
                                 Margin="0,8,0,0"
                                 Style="{StaticResource TextBoxStyle}"
                                 Text="{Binding Indirizzo.Citta, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

                        <TextBlock Grid.Row="4"
                                   Grid.Column="0"
                                   Style="{StaticResource LabelStyle}"
                                   Text="Stato:"
                                   Margin="0,8,0,0" />
                        <TextBox Grid.Row="4"
                                 Grid.Column="1"
                                 Margin="0,8,0,0"
                                 Style="{StaticResource TextBoxStyle}"
                                 Text="{Binding Indirizzo.Stato, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
                    </Grid>

                    <!-- Preview compatto dell'indirizzo -->
                    <Border Margin="0,16,0,0"
                            Background="#FFF8F8F8"
                            Padding="8"
                            CornerRadius="4"
                            BorderBrush="#FFEFEFEF"
                            BorderThickness="1">
                        <StackPanel>
                            <TextBlock FontWeight="SemiBold"
                                       Foreground="#444"
                                       Text="Anteprima Indirizzo" />
                            <TextBlock Foreground="#666"
                                       Text="{Binding Indirizzo, Converter={StaticResource IndirizzoToStringConverter}}" />
                        </StackPanel>
                    </Border>
                </StackPanel>
            </Grid>
        </Border>

        <!-- Footer: messaggi / busy -->
        <Grid Grid.Row="2"
              Margin="0,10,0,0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="*" />
                <ColumnDefinition Width="Auto" />
            </Grid.ColumnDefinitions>

            <TextBlock VerticalAlignment="Center"
                       Foreground="#666"
                       Text="{Binding StatusMessage}" />
            <StackPanel Orientation="Horizontal"
                        Grid.Column="1">
                <Button Style="{StaticResource SecondaryButton}"
                        Command="{Binding NuovoCommand}"
                        Visibility="Collapsed">Test</Button>
            </StackPanel>
        </Grid>

        <!-- Optional busy overlay -->
        <Grid Visibility="{Binding IsBusy, Converter={StaticResource BoolToVisibilityConverter}, FallbackValue=Collapsed}">
            <Rectangle Fill="#80000000" />
            <StackPanel HorizontalAlignment="Center"
                        VerticalAlignment="Center">
                <ProgressBar Width="200"
                             Height="16"
                             IsIndeterminate="True" />
                <TextBlock Foreground="White"
                           Margin="0,8,0,0"
                           Text="Operazione in corso..."
                           HorizontalAlignment="Center" />
            </StackPanel>
        </Grid>

    </Grid>
</Window>
