<UserControl x:Class="CiccioGest.Presentation.WpfMetroApp.Views.CategoriaView"
             xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
             xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
             xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
             xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
             xmlns:local="clr-namespace:CiccioGest.Presentation.WpfMetroApp.Views"
             xmlns:i="http://schemas.microsoft.com/xaml/behaviors"
             xmlns:viewmodel="clr-namespace:CiccioGest.Presentation.Mvvm.ViewModels;assembly=CiccioGest.Presentation.Mvvm"
             mc:Ignorable="d"
             d:DataContext="{d:DesignInstance Type=viewmodel:CategoriaViewModel}"
             d:DesignHeight="450"
             d:DesignWidth="800"
             d:Background="White">

    <!-- Title="Categorie" -->

    <i:Interaction.Triggers>
        <i:EventTrigger EventName="Loaded">
            <i:InvokeCommandAction Command="{Binding LoadedCommand, Mode=OneWay}" />
        </i:EventTrigger>
    </i:Interaction.Triggers>

    <Grid Margin="12">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto" />
            <RowDefinition Height="Auto" />
            <RowDefinition Height="*" />
        </Grid.RowDefinitions>

        <DockPanel Grid.Row="0">

            <TextBlock Text="Categoria"
                       FontSize="28"
                       DockPanel.Dock="Left"
                       Margin="5,5,5,5" />

            <Grid DockPanel.Dock="Right">
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="Auto" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Label Content="Cerca"
                       VerticalAlignment="Center"
                       FontSize="14" />
                <TextBox Grid.Column="1"
                         Height="23"
                         Text="{Binding Categoria.Nome, Mode=TwoWay}"
                         VerticalAlignment="Center"
                         Width="140" />
            </Grid>

            <ToolBar ToolBarTray.IsLocked="True">
                <Button Command="{Binding NuovaCategoriaCommand}">
                    <TextBlock>
                        <Run Text="&#xE8A5;"
                             FontFamily="Segoe MDL2 Assets" />
                        <Run Text="Nuova" />
                    </TextBlock>
                </Button>
                <Button Command="{Binding SalvaCategoriaCommand}">
                    <TextBlock>
                        <Run Text="&#xE74E;"
                             FontFamily="Segoe MDL2 Assets" />
                        <Run Text="Salva" />
                    </TextBlock>
                </Button>
                <!--<Button Command="{Binding ModificaCategoriaCommand}">
                    <TextBlock>
                        <Run Text="&#xE70F;"
                             FontFamily="Segoe MDL2 Assets" />
                        <Run Text="Modifica" />
                    </TextBlock>
                </Button>-->
                <Button Command="{Binding EliminaCategoriaCommand, Mode=OneWay}">
                    <TextBlock>
                        <Run Text="&#xE74D;"
                             FontFamily="Segoe MDL2 Assets" />
                        <Run Text="Elimina" />
                    </TextBlock>
                </Button>
                <!--<Button Command="{Binding AnnullaModificheCategoriaCommand}">
                    <TextBlock>
                        <Run Text="&#xE711;"
                             FontFamily="Segoe MDL2 Assets" />
                        <Run Text="Annulla" />
                    </TextBlock>
                </Button>-->
            </ToolBar>

        </DockPanel>

        <!-- Editor corrente -->
        <Border Grid.Row="1"
                Background="White"
                CornerRadius="6"
                Padding="12"
                BorderBrush="#FFE5E5E5"
                BorderThickness="1"
                Margin="0,0,0,12">
            <Grid>
                <Grid.ColumnDefinitions>
                    <ColumnDefinition Width="130" />
                    <ColumnDefinition Width="*" />
                    <ColumnDefinition Width="Auto" />
                </Grid.ColumnDefinitions>
                <Grid.RowDefinitions>
                    <RowDefinition Height="Auto" />
                    <RowDefinition Height="Auto" />
                </Grid.RowDefinitions>

                <!--Style="{StaticResource HeaderText}"-->
                <TextBlock Grid.Row="0"
                           Grid.ColumnSpan="2"
                           Text="Categoria Corrente"
                           Margin="0,0,0,12" />

                <!-- Campo ID spostato a destra -->
                <StackPanel Grid.Row="0"
                            Grid.Column="2"
                            Orientation="Horizontal"
                            VerticalAlignment="Center"
                            Margin="20,0,0,0">
                    <TextBlock VerticalAlignment="Center"
                               Foreground="#666"
                               Margin="0,0,8,0"
                               Text="ID:" />
                    <TextBlock VerticalAlignment="Center"
                               FontWeight="SemiBold"
                               Text="{Binding CategoriaCorrente.Id}" />
                </StackPanel>

                <!--Style="{StaticResource LabelStyle}"-->
                <TextBlock Grid.Row="1"
                           Grid.Column="0"
                           Text="Nome:" />

                <!--Style="{StaticResource TextBoxStyle}"-->
                <TextBox Grid.Row="1"
                         Grid.Column="1"
                         Grid.ColumnSpan="2"
                         Text="{Binding CategoriaCorrente.Nome, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />
            </Grid>
        </Border>


        <Border Grid.Row="2"
                Background="White"
                CornerRadius="6"
                Padding="12"
                BorderBrush="#FFE5E5E5"
                BorderThickness="1">

            <ListView ItemsSource="{Binding Categorie, Mode=OneWay}"
                      SelectionMode="Single"
                      SelectedItem="{Binding CategoriaSelezionata}">
                <ListView.ItemTemplate>
                    <DataTemplate>
                        <TextBlock Text="{Binding Nome}" />
                    </DataTemplate>
                </ListView.ItemTemplate>
                <i:Interaction.Triggers>
                    <i:EventTrigger EventName="MouseDoubleClick">
                        <i:InvokeCommandAction Command="{Binding SelezionaCategoriaCommand, Mode=OneWay}" />
                    </i:EventTrigger>
                </i:Interaction.Triggers>
            </ListView>

        </Border>

    </Grid>

</UserControl>
