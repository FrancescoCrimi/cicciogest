<Page
    x:Class="CiccioGest.Presentation.AppUwp2.View.ShellPage"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:local="using:CiccioGest.Presentation.AppUwp2.View"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:i="using:Microsoft.Xaml.Interactivity"
    xmlns:ic="using:Microsoft.Xaml.Interactions.Core"
    mc:Ignorable="d"
    DataContext="{Binding Main, Source={StaticResource Locator}}"
    Background="{ThemeResource ApplicationPageBackgroundThemeBrush}">

    <Grid>

        <NavigationView
            x:Name="navigationView"
            IsBackButtonVisible="Visible"
            IsSettingsVisible="True">
            <NavigationView.MenuItems>
                <NavigationViewItem Content="Nuova Fattura" Icon="Document" Tag="FatturaPage"/>
                <NavigationViewItem Content="Fatture" Icon="Document"  Tag="SelectFatturaPage"/>
                <NavigationViewItem Content="Clienti" Icon="Document" Tag="Clienti" />
            </NavigationView.MenuItems>
            <i:Interaction.Behaviors>
                <ic:EventTriggerBehavior EventName="ItemInvoked">
                    <ic:InvokeCommandAction Command="{Binding ItemInvokedCommand}" />
                </ic:EventTriggerBehavior>
            </i:Interaction.Behaviors>
            <ScrollViewer>
                <Frame x:Name="ContentFrame" Padding="12,0,12,24" IsTabStop="True"/>
            </ScrollViewer>
        </NavigationView>

    </Grid>
</Page>
